import { decideByScore, specialPartPolicy } from './共通.js';
const profile = { reason: '疑惑と知りすぎを抑え、準備と退路を優先する', advanceReason: '余計な反応をせず場をやり過ごす', labels: { conceal: '沈黙・隠蔽', prepare: '準備・退路', investigate: '探索', direct: '目立つ発言', harmful: '危険な行為' }, weights: { conceal: 5, prepare: 4, safe: 2, investigate: -2, direct: -4, harmful: -1 }, contextWeights: { conceal: 2 }, detailWeight: -0.5, detailReason: '細かい説明は注目を招くため抑える', specificityWeight: -0.1, specificityReason: '目立たない簡潔な文言を補助基準にする', indexWeight: -0.12, indexReason: '同程度なら先にある無難な対応へ寄せる', partAdjust: specialPartPolicy('hide'), adjust({ observation, labels }) { const p = observation.params || {}; const grounds = []; let score = 0; if ((p.suspicion || 0) >= 35) { score += labels.conceal * 4 + labels.safe * 2 - labels.direct * 3 - labels.investigate * 2; grounds.push('疑惑が高いため目立つ行動を避ける'); } if ((p.conviction || 0) >= 60) { score += labels.prepare * 2 + labels.conceal * 2; grounds.push('確信度が高いため防御を厚くする'); } if ((p.overknow || 0) >= 1) { score -= labels.direct * 4; grounds.push('知りすぎを重ねない'); } return { score, grounds }; } };
export const persona = { id: 'bannin', name: '保身の犯罪者', description: '倒叙・緊張の層。観測した疑惑・確信度・知りすぎに応じ、目立たず犯行準備と退路を選ぶ。', decide(o, m) { return decideByScore(o, m, profile); } };
