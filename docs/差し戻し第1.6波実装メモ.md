# 差し戻し第1.6波実装メモ

## 変更したファイル

- `src/systems/freeaction/index.js`
- `test/freeaction-risk.test.js`
- `docs/差し戻し第1.6波実装メモ.md`

指定どおり `src/main.js`、`src/systems/common.js`、`src/data/scenario/index.js` は編集していない。

## F-1: 知りすぎの希少性

`overknow +1` を残した焦点は各章一つだけである。

| 章 | 残した焦点 | 理由 |
| --- | --- | --- |
| 第一章 | `study` / 「最終章の綴じを見る」 | 本来の作者が替えたはずのない最終章の紙と綴じを、司書として即座に読んでしまう。主人公しか持ちえない知識に最も直結する。 |
| 第二章 | `morgue` / 安置室へ入ること自体 | 医師の発表前に死因へ踏み込む行為そのものが、最も明確な「知るはずのないこと」である。焦点側の二重加算は取り除いた。 |

全行動と、各行動で最も `overknow` が増える焦点を順に通すテストを追加した。実際のブロック予算より厳しい全件列挙でも、第一章は1、第二章は1、累計は2である。

| 外した箇所 | 置換後の代償 |
| --- | --- |
| `housekeeper` / 「地下の音をたずねる」 | 警戒 +1（良心 +1 は既存のまま） |
| `accomplice_room` / 「余白の注記まで読む」 | 警戒 +1（信頼 -1・疑い +6 は既存のまま） |
| `observe_detectives` / 「コウ君の靴の読み方を見る」 | 警戒 +1（行動本体の警戒 +1・疑い +7 は既存のまま） |
| `explore` / 「笹の向こうの旧道を見る」 | 疑い +3（行動本体の疑い +5 と合わせて +8） |
| `morgue` / 「爪の根元の黒さを読む」 | 追加パラメータなし。行動本体の知りすぎ +1・疑い +9 を維持。 |
| `doctor` / 「佐伯先生の言いよどみを読む」 | 疑い +3（行動本体の疑い +5 と合わせて +8） |
| `library` / 「最終章の紙を撫で比べる」 | 警戒 +1（行動本体の疑い +6 は既存のまま） |

代償文の一致テストは、`risk` だけでなく全 `scenes.focus.options[].effects` のパラメータも集計するよう拡張した。

## F-2: B-2 の調査結果

修正はしていない。F-1による表示文・代償変更後に依頼者が再計測し、その結果で判断する。

`git show HEAD:tools/AIプレイ/ログ/サマリ.json` では、`kanjou` と `shoshinsha` はともに `endingId: b2`、`acrostic` 所持、共同推理の `broken: 10` だった。現在の `tools/AIプレイ/ログ/サマリ.json` では両者とも `endingId: b1`、`acrostic` 所持、`other_plan`・`three_plans`・`three_plans_card` を新規取得し、共同推理の `broken: 11` である。一方、基準線にあった `hidden_dimension` と `old_road` は失っている。

現行ログでは、`kanjou` は第一章で「家政婦と話す」→「探偵一行を観察する」→「共犯者の部屋へ」→「書斎を調べる」と進み、共犯者の紙について「二枚は別の場所へ送り出されている」と読んでいる（`tools/AIプレイ/ログ/kanjou.md:1111-1181`）。`shoshinsha` も第一章の最後に共犯者の部屋を選び、「恩田さんの名の脇に『読ませるな』」を読んでいる（`tools/AIプレイ/ログ/shoshinsha.md:1148-1181`）。この自由行動の選択変更が `three_plans` 系を増やし、n11 を含む11ノード全てを崩せる状態を作った。

これは `n10` の `isB2Locked` 経路ではない。両基準線とも `acrostic` を所持しており、n10ロックの条件（折句なし）を満たさないためである。基準線の `broken: 10` は別の崩せないノードで止まったことを示す。検証基盤の `actInference()` はその状態で `.parts-close` を押し、`inference:skipped-unbreakable-node` としてB-2を返す。現在は11ノードを完遂するため最終選択へ進み、基盤は「帰り唄」を優先してB-1を選ぶ。

従って、実プレイで観測されていたB-2は「カードを揃えた上で `condemn` / `silent` を選ぶ」積極的な未完ではなく、少なくともこの二ペルソナでは崩せないノードから閉じる失敗経由だった。`inference-final.js` の `condemn` と `silent` は依然として到達可能だが、検証基盤もペルソナも `meaning`（帰り唄）を優先するため、今回の修正だけでB-2の実プレイ踏破が回復する保証はない。これは依頼者判断待ちとして、勝手なEND条件・ペルソナ採点の変更は行わない。

## DOM契約の変更申告

変更なし。

## 実装しなかったこと

- Chromeが必要な `tools/ブラウザ検証/実行.sh` と `tools/AIプレイ/実行.sh all` は実行していない。
- F-2のEND条件、`inference-final.js` の最終選択、ペルソナ採点は変更していない。

## 実ブラウザで確認すること

1. 第一章で書斎を選び「最終章の綴じを見る」を選ぶ。代償に疑いと知りすぎが表示され、知りすぎが1増えることを確認する。
2. 第二章で安置室へ入り、任意の焦点を選ぶ。知りすぎは行動一回分だけ増え、焦点で二重加算されないことを確認する。
3. 第一章の地下の音・共犯者の注記・靴・旧道、第二章の医師の言いよどみ・最終章の紙を順に選び、それぞれの代償表示と実効果が知りすぎではないことを確認する。
4. 上記を含め踏み込める行動を選び続けても、第二章終了時の知りすぎが2以下であることを確認する。
5. `tools/ブラウザ検証/実行.sh` で execution=`a4` / confession=`b1` / `errors: []` / `overflow: []` を再確認する。
6. `tools/AIプレイ/実行.sh all` を再計測し、10ペルソナで全7END（特にB-2）が再び踏破されるか確認する。B-2がなお0人なら、上記の失敗経由依存を踏まえて、積極的な `condemn` / `silent` 到達をどう検証に含めるかを判断する。

## 検証コマンドと出力

2026-07-27に実行した。

```text
$ node --test
# tests 14 / # pass 14 / # fail 0

$ node tools/パート終端検査.mjs
手毬唄ボード: 6操作で終端
反論: 3操作で終端
自由行動: 6操作で終端

$ node tools/シナリオ検証.mjs
シナリオ検証: 不整合0件

$ node tools/統合検証.mjs
統合検証: シーン13件・7END・遷移を確認
アセット欠落 0件: プレースホルダで継続可能

$ node tools/到達可能性検査.mjs
A-1: a1_arrest
A-4: a4_reversal
B-1: b1_true
B-2: b2_unfinished

$ node tools/END到達条件検査.mjs
END到達条件検査（自由行動予算: 第一章4/6、第二章4/5）
- 自由行動組合せを 75 通り探索
全7ENDがブロック予算・選択分岐を含む経路で到達可能です。

$ tools/構文検査.sh
ESM構文検査: 66 ファイル、エラー 0
```
